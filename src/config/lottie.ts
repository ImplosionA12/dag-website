export const LOTTIE_CONFIG = {
  // Replace null with path string when file is ready e.g. '/lottie/dag-logo.json'
  logoAnimation:   null as string | null,
  loadingScreen:   null as string | null,
  trophyAnimation: null as string | null,
  characterIdle:   null as string | null,
}
